DBM temp + queue stability update (pre-network work)

- Centralized TEMP deletion to flushOne; all TEMP deletions now go through forced flush path
- Normalize queue cache keys on Windows (lowercase paths) to prevent duplicate entries
- makeTemporary now refuses if base or journal already exists on disk
- Expanded TestingGrounds: temp lifecycle, rename, batch threshold tests; then crash-test harness

Current state
- DBM temp cache semantics: temp stays cache-only, auto-deletes on inactivity, never persists
- PERM files retain patch/materialize lifecycle; temp cannot be applied once on disk
- QueueManager maintenance runs continuously; no manual flushAll required for temp cleanup

Crash test instructions
- Run bot with TestingGrounds crash test, terminate after ~2 minutes (no shutdown)
- Next run should trigger CrashHandler recovery for any journaled PERM files
